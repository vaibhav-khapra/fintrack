/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
        turbo: {}, // optional
    },
    output: 'export',
    
};

export default nextConfig;
